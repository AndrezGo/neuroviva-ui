/**
 * Domain types for user accessibility preferences.
 * Pure TypeScript — no imports from other layers.
 */

export interface AccessibilityPreferences {
  largeText: boolean;
  highContrast: boolean;
}
